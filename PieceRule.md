# 棋子規則

## 排列

- 棋子放在固定長度 N+1 的 list 中
- 棋子數 N, 置中有一空白

## 判斷可以移動的的棋子

- 棋子位置 i, 則 i+1 或 i-1 有空格, 則該棋子可以移動
- 棋子位置 i, 則 i+1 或 i-1 有另一顆棋子, i+2 或 i-2 有空格, 則該棋子可以移動

## 移動棋子

- 空白左側的棋子要移動到右邊
- 空白右側的棋子要移動到左邊
- 棋子每回合只能移動一次

## 跳躍棋子

- 左側棋子往右邊移動時, 若位置 i 前方 i+1 處有棋子, 則移動到 i+2, 若 i+2 有棋子則無法移動
- 右側棋子往左邊移動時, 若位置 i 前方 i-1 處有棋子, 則移動到 i-2, 若 i-2 有棋子則無法移動
- 若棋子無法移動則應回退到上上一步, 重新判斷可動棋子
- 棋子每回合只能移動一次

## 完成移動

- 當左邊棋子們全數依序移動到空格的右邊, 則左邊完成移動
- 當右邊棋子們全數依序移動到空格的左邊, 則右邊完成移動
- 當左右二邊棋子皆完成移動, 則遊戲結束