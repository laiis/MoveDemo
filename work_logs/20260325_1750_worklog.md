# 工作日誌 - 2026-03-25: 重構搜尋工具與準備 A* 實作

## 目前進度
- 發現 `AStarStrategy.getNeighbors` 尚未實作。
- 已將 `BFSStrategy` 中的鄰居搜尋與棋盤交換邏輯抽取至 `SearchUtils`，以供所有搜尋演算法共用。
- 優化了 `SearchUtils.getBoardString` 的實現，確保其生成的 `S`、`L`、`R` 字符與搜尋演算法一致。
- 擴展了 `StrategyTest.testBFSStrategy` 的測試範圍（加入 $N=10, 20$），以驗證大數據集下的失敗。

## 發現與觀察
1. **工具方法共享**: `SearchUtils` 現在提供完整的搜尋輔助功能，包含目標狀態判定與鄰居生成。
2. **搜尋狀態同步**: `State` 物件中的 `spaceIdx` 現在可被精確計算，減少 `indexOf` 的頻繁呼叫。

## 後續待辦
- 修改 `AStarStrategy` 呼叫 `SearchUtils.getNeighbors`。
- 更新 `Heuristics` 以支援字串棋盤狀態，減少物件建立成本。
- 修復 `AStarStrategy` 啟發函數邏輯並驗證測試。
