# 工作日誌 - 2026-03-25: 調查 BFSStrategy 與 AStarStrategy 測試失敗

## 目前進度
- 重現了 `StrategyTest` 中的失敗狀況。
- `testBFSStrategy` 在 $N=20$ 時顯示為 `SKIPPED` 或 `FAILED`（依環境而定，主要原因為狀態空間過大導致執行時間過長或記憶體壓力）。
- `testAStarStrategy` 在 $N=1$ 到 $N=5$ 均失敗，初步判斷可能是 A* 實作邏輯不完整或啟發函數問題。
- 確認 `SearchUtils.isGoal` 定義正確（目標為 `R...SL...`）。

## 發現與觀察
1. **BFS 效能瓶頸**: 對於 $N=20$，棋盤長度為 41，BFS 的狀態空間為 $\frac{41!}{20!20!1!}$，這是一個天文數字。單純的 BFS 無法在合理時間內完成 $N=20$ 的計算。
2. **A* 錯誤**: 即使在小規模 $N$ 下，`AStarStrategy` 依然失敗，需進一步檢查 `AStarStrategy.java` 及其啟發函數。

## 後續待辦
- 優化 `BFSStrategy` 或調整測試規模。
- 修復 `AStarStrategy` 邏輯。
- 確保所有策略符合 $N(N+2)$ 步數要求。
