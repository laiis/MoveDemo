# 工作日誌 - 2026-03-27: 規則同步與搜尋演算法最終優化

## 目前進度
- **同步 PieceRule 規範**: 修正 `PieceRule.md` 與程式碼實作，嚴格禁止跳過同向棋子。
- **修正 StepImpl.java**: 實作跳躍對象的方向檢查，確保物件導向模型與業務規則一致。
- **優化 A* 搜尋策略**:
    - 重構 `AStarStrategy` 使用 `PriorityQueue` 實作標準 A*。
    - 在 `SearchUtils.getNeighbors` 中引入「跳躍優先」剪枝策略，大幅縮減狀態搜尋空間。
    - 成功使 A* 策略在 $N=26$ 規模下快速求解，且步數精確符合 $N \times (N+2) = 728$。
- **全面通過測試**:
    - `PieceRuleTest`: 驗證禁止跳過同向棋子的規則。
    - `StrategyTest`: `TheBestStrategy`, `BFSStrategy`, `AStarStrategy`, `RecursionStrategy` 均已通過驗證（其中 A* 效能顯著提升）。

## 發現與觀察
1. **規則的重要性**: 規則的小幅變動（禁止跳過同向棋子）能有效減少無效的狀態分支，對搜尋演算法的效能提升有巨大幫助。
2. **啟發函數與剪枝的結合**: A* 本身的 $h(s)$ 配合「跳躍優先」的貪婪策略，能在極短時間內找到最優路徑，大幅降低記憶體開銷。

## 後續待辦
- 檢視全專案的 JavaDoc 與程式碼規範，確保符合《專案憲章》要求。
- 優化 CLI 輸出，在解決問題時能清晰列出移動路徑。
